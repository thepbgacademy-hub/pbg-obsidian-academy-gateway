# Build Error Log

## 2026-05-12

- `npm install` completed, but npm reported 5 moderate dependency audit findings in the current dev toolchain. No fix applied because the assignment pins the Phase 0 dependency set and `npm audit fix --force` would make breaking dependency changes.
- Phase 0 red test: `npm test -- packages/shared/tests/contracts.test.ts` failed because `packages/shared/src/contracts.ts` did not exist. This was the expected TDD failure before adding the shared contract implementation.
- Phase 0 typecheck: `npm run typecheck` failed with TS2835 because `moduleResolution: "NodeNext"` requires explicit `.js` extensions for relative ESM imports. Fixed by changing test imports to use `.js` specifiers, which TypeScript maps back to the `.ts` source.
- Phase 1 database verification: live migration execution could not be run because `DATABASE_URL` is not set and `psql` is not available on PATH in this environment. Per assignment instructions, performed SQL structure review instead: migration includes the `pbg_obsidian` schema, all five blueprint tables, references to `academy_core.students(id)`, the one-active-device partial unique index, and RLS enables for each table.
- Phase 2 red test: `npm test -- packages/shared/tests/courseManifest.test.ts` failed because `packages/shared/src/courseManifest.ts` did not exist. This was the expected TDD failure before adding the course manifest implementation.
- Phase 2 contract red test: `npm test -- packages/shared/tests/contracts.test.ts` failed because `API_ROUTES` was not exported yet. Fixed by adding shared route constants to `packages/shared/src/contracts.ts`.
- Phases 0-2 review fix: OpenAPI documented only four shared API routes while `API_ROUTES` exposed nine current POC route constants. Updated `docs/api/openapi-poc.yaml` to include all shared routes, including the OpenAPI `{runId}` equivalent of `/api/workflows/runs/:runId`.
- Phases 0-2 review fix: course manifest hashes used placeholder strings. Added a manifest hash regression test using Node `crypto` and replaced each placeholder with the deterministic SHA-256 digest of its body.
- Phases 0-2 review fix: `@pbg/shared` package metadata exposed a bare TypeScript source path as `main` only. Added source-module `exports` and `types` mappings for TS workspace consumers without adding build output.
- Phase 3 red test: `npm test -- packages/gateway-api/tests/gatewayRoutes.test.ts` failed because `packages/gateway-api/src/app.ts` did not exist yet. This was the expected TDD failure before adding the Fastify `buildApp` implementation.
- Phase 3 dependency install: `npm install` completed after adding the `@pbg/gateway-api` workspace, but npm reported 10 audit findings in the current dependency tree. No audit fix was applied because that could alter dependency versions outside the Phase 3 API skeleton scope.
- Phase 3 review fix red test: `npm test -- packages/gateway-api/tests/gatewayRoutes.test.ts` failed with 404 responses for `/api/auth/login` and `/api/dashboard/me`. Fixed by adding lightweight local POC auth and dashboard route stubs with seeded credentials only; no real DB/auth was introduced.
- Phase 4 red test: `npm test -- packages/obsidian-plugin/tests/onboarding.test.ts` failed because `packages/obsidian-plugin/src/onboarding.ts` did not exist. This was the expected TDD failure before adding the Obsidian plugin onboarding constants.
- Phase 4 dependency install: `npm install` completed after adding the `pbg-academy-gateway` workspace, but npm reported 10 audit findings in the current dependency tree. No audit fix was applied because `npm audit fix --force` may make dependency changes outside the Phase 4 plugin shell scope.
- Phase 4 typecheck: `npm run typecheck` failed because `dashboardView.ts` read `containerEl.children[1]`, which is possibly undefined under strict TypeScript. Fixed by rendering into Obsidian `ItemView.contentEl`.
- Phase 4 manual verification limitation: Obsidian app UI could not be visually inspected from this worker session. Verified installer output on disk instead: `E:\Obsidian\PBG Plug in\.obsidian\plugins\pbg-academy-gateway\manifest.json`, `styles.css`, and `main.js` exist after running `.\scripts\install-plugin.ps1`.
- Phase 5 red test: `npm test -- packages/obsidian-plugin/tests/courseSync.test.ts` failed because `packages/obsidian-plugin/src/courseSync.ts` did not exist. This was the expected TDD failure before adding the manifest write planner.
- Phase 5 live endpoint check: `http://localhost:8787/api/courses/manifest` returned the existing listener's `NOT_FOUND` payload because port 8787 is already held by Docker/WSL processes in this environment. Verified the built gateway manifest route on alternate port 8788 with `Invoke-WebRequest`, returning HTTP 200 and the POC manifest.
- Phase 5 review fix red test: `npm test -- packages/obsidian-plugin/tests/settings.test.ts` failed because plugin settings helpers did not exist. Fixed by adding a persisted `gatewayBaseUrl` setting with default `http://localhost:8787`, a settings tab override input, and using the configured value for `Sync PBG Course Manifest`.
- Phase 6 red tests: `npm test -- packages/gateway-api/tests/authRoutes.test.ts` showed injected auth service responses were ignored by the existing seeded login route; `npm test -- packages/gateway-api/tests/sessions.test.ts` failed because `src/auth/sessions.ts` did not exist; `npm test -- packages/obsidian-plugin/tests/apiClient.test.ts` failed because `PbgGatewayApiClient` did not exist. Fixed by adding injectable auth service contracts with seeded fallback, session token helpers, and a fetch-injectable plugin API client.
- Phase 6 test fixture fix: `npm test -- packages/gateway-api/tests/sessions.test.ts` initially failed after implementation because the expected SHA-256 digest for `poc-refresh-token` in the new test was incorrect. Fixed the test fixture to the actual hex digest returned by Node crypto.
- Phase 6 review fix red tests: `npm test -- packages/obsidian-plugin/tests/settings.test.ts packages/gateway-api/tests/authRoutes.test.ts` failed because the settings tab URL update path had no token-preserving helper and the advertised refresh, logout, and device registration routes returned 404. Fixed by adding `updateGatewayBaseUrl`, using it from the settings tab, and adding lightweight store-free POC route handlers for refresh/logout/device registration.
- Phase 6 re-review fix: seeded POC login returned `device-refresh-token` while `/api/auth/refresh` accepted only `poc-refresh-token`. Fixed by exporting a single internal `POC_REFRESH_TOKEN` constant, using it in the seeded login response and auth tests, and adding a login-then-refresh regression test that refreshes with the returned token.
- Phase 7 red tests: `npm test -- packages/obsidian-plugin/tests/contextPreview.test.ts packages/obsidian-plugin/tests/workflowActions.test.ts packages/obsidian-plugin/tests/apiClient.test.ts` failed because `contextPreview.ts` and `workflowActions.ts` did not exist and `PbgGatewayApiClient.runAssignmentCoach` was not implemented. This was the expected TDD failure before adding the explicit Assignment Coach workflow implementation.
- Phase 7 typecheck: `npm run typecheck` failed because `workflowActions.test.ts` passed small fakes directly to parameters typed as Obsidian `TFile` and `Vault`. Fixed by keeping the production helper typed to real Obsidian types and casting only the small test fakes at the test call boundary.
- Phase 7 privacy/tag review red test: `npm test -- packages/obsidian-plugin/tests/workflowActions.test.ts` failed because `runAssignmentCoachForFile` read and sent `Private/journal.md`, and YAML `tags: [academy]` plus `tags: academy` were not detected. Fixed by enforcing the Assignment Coach scope before `vault.read`/client calls, guarding the active-note command with the same scope check, and extending simple frontmatter tag detection for common academy tag forms.
- Phase 7 result path review red test: `npm test -- packages/obsidian-plugin/tests/workflowActions.test.ts` failed because unsafe run IDs like `../outside` and `foo/bar` were interpolated into `PBG/Workflow Results/${runId}.md`. Fixed by validating result filename stems against alphanumeric/dash/underscore characters and using the deterministic `assignment-coach-result` fallback for invalid run IDs.
- Phase 8 red tests: `npm test -- packages/obsidian-plugin/tests/vaultScope.test.ts packages/obsidian-plugin/tests/localState.test.ts` failed because `src/vaultScope.ts` and `src/localState.ts` did not exist yet. This was the expected TDD failure before adding scoped path normalization and local dashboard metrics helpers.
- Phase 8 scope fix: `npm test -- packages/obsidian-plugin/tests/vaultScope.test.ts packages/obsidian-plugin/tests/localState.test.ts` failed because `PBG/` normalized to an invalid empty segment path. Fixed normalization to accept one trailing folder slash while still rejecting interior empty segments such as `PBG//Assignments/a.md`.
- Final review red tests: `npm test -- packages/gateway-api/tests/gatewayRoutes.test.ts` failed because dashboard, course manifest, Assignment Coach run, and workflow run lookup were not gated by Bearer auth; the advertised Assignment Coach preview and workflow run lookup routes were missing; and Assignment Coach accepted out-of-scope paths such as `Private/journal.md`. Fixed by accepting the seeded POC access token returned by login/refresh, requiring it on protected routes, adding preview and lookup stubs, and rejecting non-`PBG/Assignments/*.md` run requests before returning a workflow response.
- Final review red tests: `npm test -- packages/obsidian-plugin/tests/apiClient.test.ts packages/obsidian-plugin/tests/installScripts.test.ts` failed because the plugin API client did not send Authorization headers or expose preview/run lookup helpers, and the install/smoke scripts wrote to a hard-coded vault path without validation or confirmation. Fixed by persisting login access tokens in the client, sending Bearer headers when present, adding client helpers, parameterizing `VaultPath`, validating `.obsidian`, and requiring `-ConfirmInstall`/ShouldProcess before install writes.
- Whole-build re-review red test: `npm test -- packages/gateway-api/tests/gatewayRoutes.test.ts` failed because authenticated Assignment Coach preview requests accepted out-of-scope `Private/journal.md` and non-markdown `PBG/Assignments/journal.txt` paths. Fixed by applying the same `getAssignmentScopeError` guard used by the run route to `POST /api/workflows/assignment-coach/preview`.
- Phases 9-13 gateway hardening red tests: `npm test -- packages/gateway-api/tests/authRoutes.test.ts packages/gateway-api/tests/gatewayRoutes.test.ts packages/gateway-api/tests/serverConfig.test.ts` failed as expected because the gateway did not yet export server startup config, revoke POC sessions, require auth for device registration, validate workflow request shape, enforce injectable workflow authorization, capture security audit events, or rate limit repeated requests. Fixed in the gateway API with in-memory POC services and focused tests.
- Phases 9-13 verification limitation: `npm run typecheck` failed in `packages/obsidian-plugin/tests/esbuildConfig.test.ts` because TypeScript has no declaration for `packages/obsidian-plugin/esbuild.config.mjs`. This is outside Worker A's owned paths and was not changed.
- Phases 9-13 build limitation: `npm run build --workspace @pbg/shared` failed because `@pbg/shared` has no `build` script. Gateway API package build passed with `npm run build --workspace @pbg/gateway-api`.
- Phase 14 integration fix: the prior `npm run typecheck` limitation was resolved after the plugin esbuild config test settled under the repo-wide typecheck. Full `npm run typecheck` passed.
- Phase 13 dependency audit fix: controlled upgrades to Fastify, Vitest, and esbuild cleared the high/moderate audit findings. `npm audit --audit-level=high` returned `found 0 vulnerabilities`, and full `npm test`, `npm run typecheck`, and `npm run build` passed afterward.
- Integration review red test: `npm test -- packages/gateway-api/tests/gatewayRoutes.test.ts` failed after adding a session-student dashboard regression because `/api/dashboard/me` still returned seeded POC student values instead of the authenticated session's student. Fixed the route to derive dashboard student fields from `request.authSession.student`.
- Final review red tests: `npm test -- packages/gateway-api/tests/authRoutes.test.ts packages/gateway-api/tests/gatewayRoutes.test.ts` failed because malformed login requests returned 401 after reaching auth service code, authenticated malformed device registration returned 200 with missing fields, and invalid Assignment Coach task count invariants returned 200. Fixed by adding runtime validators for login, device registration, and nonnegative Assignment Coach task counts with `completedTaskCount <= taskCount`.
- Final verification fix: parallel `npm test`, `npm run typecheck`, and `npm run build` surfaced a TypeScript narrowing error in the new task-count invariant helper and caused two Vitest tests to time out under parallel compile load. Fixed the narrowing with a dedicated `hasValidTaskCounts` helper, then reran verification sequentially: `npm test`, `npm run typecheck`, and `npm run build` all passed.

## 2026-05-15

- Hermes slice red test: `npm test -- packages/shared/tests/contracts.test.ts` failed because `API_ROUTES.dashboardAnnouncements` was undefined. Fixed by adding the shared `/api/dashboard/announcements` route constant in `packages/shared/src/contracts.ts`.
- Hermes slice red test: `npm test -- packages/gateway-api/tests/dashboardAnnouncements.test.ts` failed with Light My Request schema output (`must have required property 'url'`) because the new dashboard announcement route constant was still undefined at inject time. Fixed by adding the shared route constant first, then wiring the authenticated gateway handler in `packages/gateway-api/src/app.ts`.
- Hermes slice red tests: `npm test -- packages/obsidian-plugin/tests/dashboardFlags.test.ts` and `npm test -- packages/obsidian-plugin/tests/announcements.test.ts` failed because `src/dashboardFlags.ts` and `src/announcements.ts` did not exist yet. Fixed by adding the default dashboard flag module and minimal announcement state helpers with three-step blink countdown plus repeated-payload stability.
- Hermes slice red test: `npm test -- packages/obsidian-plugin/tests/apiClient.test.ts` failed because `PbgGatewayApiClient.getDashboardAnnouncements` did not exist. Fixed by adding the typed gateway client method and importing the local announcement payload type.
- Hermes adaptation slice 2 red test: `npm test -- packages/obsidian-plugin/tests/dashboardShell.test.ts packages/obsidian-plugin/tests/localState.test.ts` failed because `packages/obsidian-plugin/src/dashboardShell.ts` did not exist yet. Fixed by adding the shell model, preserving the local classroom-state guardrail, and rewriting `dashboardView.ts` around the academy header, provisional left rail, main workspace cards, hidden Hermes flag regions, and click-only workflow actions.
- Hermes adaptation slice 2 verification: after wiring announcement polling, token-aware banner refresh, and the clickable header-link banner styling, `npm test -- packages/obsidian-plugin/tests/dashboardShell.test.ts packages/obsidian-plugin/tests/localState.test.ts packages/obsidian-plugin/tests/announcements.test.ts`, `npm run typecheck`, and `npm run build` all passed.
- Hermes review fix: the seeded announcement banner linked to `https://example.invalid/...`, so the first clickable banner action was broken by design. Fixed by promoting the announcement payload type into `@pbg/shared/contracts`, reusing it in the gateway and plugin client/state layers, and replacing the seeded link with the live project repository URL.
- Hermes review fix: `dashboardView.refreshAnnouncements()` could still rerender after `onClose()` if a poll resolved late. Fixed by checking `isDisposed` immediately after awaiting the gateway payload and before mutating state or restarting the blink cycle.
- Hermes review fix red test: `npm test -- packages/obsidian-plugin/tests/dashboardView.test.ts` initially failed because importing `dashboardView.ts` pulled in the runtime `obsidian` package, which Vitest cannot resolve in this environment. Fixed by extracting the pure banner-state helper into `src/dashboardBanner.ts` and testing that module directly.
- Hermes review fix: banner links were rendered without scheme validation, which could have allowed non-web protocols from remote payloads. Fixed by accepting only `http:` and `https:` URLs in `src/dashboardBanner.ts` and falling back to a neutral placeholder when the href is unsafe.
- Hermes review fix: the late-close announcement refresh path still lacked regression coverage. Fixed by adding `packages/obsidian-plugin/tests/dashboardLifecycle.test.ts`, which mocks the `obsidian` module and proves `refreshAnnouncements()` does not rerender or restart blink after the view is disposed mid-request.
- Hermes review fix: `onOpen()` could still recreate polling after `onClose()` if the view closed during initial snapshots or the first announcement refresh. Fixed by adding `isDisposed` guards after both startup awaits and before `startAnnouncementPolling()`, plus a lifecycle regression test that closes the view during startup refresh and verifies polling does not start.
- Discussion badge slice red tests: `npm test -- packages/obsidian-plugin/tests/discussionStatus.test.ts packages/obsidian-plugin/tests/dashboardRail.test.ts packages/obsidian-plugin/tests/apiClient.test.ts packages/obsidian-plugin/tests/dashboardLifecycle.test.ts` failed because `src/discussionStatus.ts` did not exist yet, `PbgGatewayApiClient` had no discussion status or mark-seen methods, `dashboardShell.ts` had no rail badge/link fields, and `dashboardView.ts` did not export the optimistic discussion click helper. Fixed by adding the discussion status state module, local discussion payload types, the two client methods, conditional rail badge/link modeling, and the click helper plus dashboard polling/render wiring.
- Discussion badge slice verification: after wiring the dashboard view to poll `/api/lounge/discussion-status`, render the `PBG Discussion` rail item, open the Telegram link, and optimistically clear the badge on click, `npm test -- packages/obsidian-plugin/tests/discussionStatus.test.ts packages/obsidian-plugin/tests/dashboardRail.test.ts packages/obsidian-plugin/tests/apiClient.test.ts packages/obsidian-plugin/tests/dashboardLifecycle.test.ts` passed with 21 tests green, `npm test -- packages/obsidian-plugin/tests/dashboardShell.test.ts packages/obsidian-plugin/tests/dashboardView.test.ts` passed with 5 tests green, and `npm run build --workspace pbg-academy-gateway` passed.
- Discussion badge slice verification concern: `npm run typecheck` initially failed in `packages/obsidian-plugin/tests/dashboardShell.test.ts` because that existing fixture no longer satisfied `AnnouncementViewState` after earlier announcement-state changes (`motionPhase` and `pendingBlink` were missing). Resolved during integration by updating the fixture to the current shape; repo-wide `npm run typecheck` is now green again.
- Discussion badge review fix: the optimistic badge clear could be undone by a stale in-flight status poll before `markDiscussionSeen()` completed. Fixed by adding a local `seenPending` flag in discussion state, suppressing stale unread resurrection while pending, and clearing the pending flag only after the gateway confirms or the mark-seen request fails.
- Discussion badge review fix: if the dashboard stayed open after the plugin lost its saved session, the last fetched `PBG Discussion` badge/link could remain on screen as a dead control. Fixed by resetting discussion rail state to its empty default whenever `refreshDiscussionStatus()` finds no authenticated gateway client, and by adding explicit unauthenticated route tests for both lounge endpoints.
- Discussion badge review fix: server-side session expiry/revocation could still leave a stale `PBG Discussion` rail item visible because the lounge poll catch path only logged `401/403` failures. Fixed by treating `401` and `403` gateway errors as a dead session for lounge polling, clearing the local discussion state in that path, and adding a lifecycle regression for invalid-token refresh behavior.
- Discussion badge review fix: click-triggered `markDiscussionSeen()` failures with `401/403` could still leave the stale `PBG Discussion` rail item visible until the next poll because the catch path only cleared the pending flag. Fixed by treating `401` and `403` mark-seen failures as dead session state too, resetting the local discussion badge immediately, and adding a regression for invalid-token click behavior.
- Discussion badge slice red test: `npm test -- packages/shared/tests/contracts.test.ts` failed because `API_ROUTES.discussionStatus` and `API_ROUTES.discussionSeen` were undefined. Fixed by adding the shared lounge route constants and the `DiscussionStatusPayload` / `DiscussionSeenResponse` types in `packages/shared/src/contracts.ts`.
- Discussion badge slice red test: `npm test -- packages/gateway-api/tests/loungeStatus.test.ts` failed with Light My Request schema errors (`must have required property 'url'`) because `API_ROUTES.discussionStatus` and `API_ROUTES.discussionSeen` were still undefined at inject time. Fixed by adding those shared route constants first, then wiring authenticated lounge status and mark-seen handlers in `packages/gateway-api/src/app.ts`.
- Discussion badge slice red test: `npm test -- packages/gateway-api/tests/gatewayRoutes.test.ts` failed on the new per-student isolation case with the same inject schema error because the discussion route constant was undefined. Fixed by adding one app-scoped in-memory discussion state keyed by `studentId` and reusing it across the new lounge routes.
- Telegram discussion integration debugging: the first real n8n-backed workflow returned `500` because it tried to use `fetch` inside an n8n Code node, which the n8n docs explicitly disallow for HTTP access. Fixed by moving Telegram `getUpdates` into an HTTP Request node, shifting the design to cached background polling every 30 minutes, and keeping a separate manual refresh webhook for clean verification without raising the production polling rate.

- Hermes palette install note: scripts\install-plugin.ps1 threw a NullReferenceException inside ShouldProcess when run with -ConfirmInstall from this shell session. Worked around by copying manifest.json, styles.css, and dist/main.js directly into E:\Obsidian\PBG Plug in\.obsidian\plugins\pbg-academy-gateway.
